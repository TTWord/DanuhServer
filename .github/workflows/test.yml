name: updateTest

on:
  push:
    branches:
      - 'develop'  # 개발 브랜치가 푸쉬되었을 때 동작

  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: SSH commands using password
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.TEST_HOST }} # 서버  ip주소
          username: ${{ secrets.TEST_USER }} # 서버 유저이름
          password: ${{ secrets.TEST_PASSWORD }} # 서버 패스워드
          port: ${{ secrets.TEST_PORT }} # 서버 포트
          script: ./update.sh
